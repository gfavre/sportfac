# Pro-tip: Try not to put anything here. There should be no dependency in
#	production that isn't in development.
-r base.txt

gunicorn==19.7.1
redis==2.10.6
raven==6.7.0
django-constance[redis] == 1.2.1
pylibmc == 1.6.3
psycopg2-binary==2.9.5
ipython